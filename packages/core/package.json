{
  "name": "@codecontext/core",
  "version": "0.1.0",
  "description": "Shared types and utilities for CodeContext",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "echo 'No linter configured'",
    "test": "vitest run --config vitest.config.cjs"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^2.1.0"
  }
}
