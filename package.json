{
  "name": "codecontext",
  "version": "0.1.0",
  "description": "CLI + web dashboard tool that scans codebases and generates AI-friendly context",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^1.13.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@8.0.0"
}
